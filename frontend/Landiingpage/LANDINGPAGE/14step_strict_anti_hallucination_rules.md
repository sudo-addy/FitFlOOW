# STRICT ANTI-HALLUCINATION RULES

These rules override everything.

- Never invent layouts.
- Never redesign reference layouts.
- Never change uploaded images.
- Never replace the subject.
- Never assume missing content.
- Never create random sections.
- Never create dashboard components.
- Never create admin UI.
- Never create login pages.
- Never use placeholder designs.
- Never add unnecessary elements.
- Always ask for clarification if information is missing.

Implement exactly what is provided.

Every section must visually connect to the previous one.

The uploaded image is always the centerpiece of the entire landing page.
